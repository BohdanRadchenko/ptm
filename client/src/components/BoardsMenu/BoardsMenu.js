import React from "react";
import css from './BoardsMenu.module.scss'

const BoardsMenu = () => {
  return (
      <div className={css.container}>
        Boards Menu
      </div>
  )
}

export default BoardsMenu